import { PagePlaceholder } from "./PagePlaceholder";

export function VotingSystem() {
  return (
    <PagePlaceholder
      title="Voting System"
      description="Create polls, collect votes, and view results in one place."
    />
  );
}
